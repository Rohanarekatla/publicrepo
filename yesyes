for /f %i in ('az account list --query "[].id" -o tsv') do @(az account set --subscription %i > nul && az network vnet list --query "[?name=='YOUR-VNET-NAME'].{Subscription:'%i', AddressSpace:addressSpace.addressPrefixes[0]}" -o tsv 2>nul)